import * as merchants from "./merchants";
import * as common from "./common";
import * as payments from "./payments";

export { merchants, common, payments };

export default { merchants, common, payments };
